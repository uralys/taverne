import test from 'ava';
import {useTaverne} from '../../src/hooks/context-provider';

test('pour(string)', t => {
  t.truthy(false);
});

test('pour([])', t => {
  t.truthy(false);
});

test('pour({fromRoot: []})', t => {
  t.truthy(false);
});

test('pour(state => ({}))', t => {
  t.truthy(false);
});
